version: "3"

services:

  app:
    image: ${APP_REGISTRY}
    volumes:
      - ${APP_STORAGE_VOLUME}:/var/www/html/storage
      - ${APP_ENV_VOLUME}:/var/www/html/.env
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - db
      - cache
    networks:
      - backend
      - frontend
